# copie pra .env e ajuste se precisar. o .env n vai pro git
#
# endereco da api. o vite le isso no momento do build, entao depois de
# mudar precisa subir o servidor de novo
VITE_API_URL=http://localhost:8080
